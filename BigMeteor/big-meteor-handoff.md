# 好大的流星 交接

更新时间：2026-05-07

## 仓库与接手

GitHub 仓库：

```text
https://github.com/xingxp/wow-plugins
```

新机器接手：

```bash
git clone https://github.com/xingxp/wow-plugins.git
cd wow-plugins
```

插件源码目录：

```text
BigMeteor
```

游戏内插件目录名必须保持：

```text
BigMeteor
```

如果新机器 WoW 路径不同，把 `BigMeteor/` 整个目录同步到该机器的：

```text
World of Warcraft/_classic_titan_/Interface/AddOns/BigMeteor
```

## 当前目标

当前阶段先稳定火/暗计量条，再加第一版右侧技能推荐：

- 正确监控目标身上的 `余烬印记（火）`
- 正确监控目标身上的 `暗影印记`
- 用左右两侧计量条把层数显示出来
- 在右侧显示“当前施法轨道 + 下一推荐轨道”

## 当前结论

- 当前已回退到 `2267366 / 0.5.18` 这一版，作为“能跑的版本”基线
- 选择这个回退点的原因：
  - 它已经包含火/暗计量条、暗灼 CD 条、右侧两行推荐轨道
  - 但还没有后续加入的预测队列、候选锁定、强兜底逻辑
  - 这些后续逻辑在实战里引入了“无推荐”“放完暗影箭没后续”“计量条不显示”等新问题
- 后续继续修复时，必须从这个基线逐块增加逻辑，不要再直接恢复 `0.5.27+` 那套预测层

## 当前文件

```text
BigMeteor.toc
BigMeteor.lua
big-meteor-handoff.md
```

工作空间源码目录：

```text
/Users/wangxin/dev/wow-plugin/BigMeteor
```

这是原开发机器路径；新机器路径不要求一致，以 clone 后的 `BigMeteor/` 为准。

当前版本：

```text
0.5.18
```

## 当前行为

- 左侧红色竖条表示目标 `火` 印记层数
- 右侧紫色竖条表示目标 `暗` 印记层数
- 两个计量条靠近显示，作为核心状态区
- 两侧计量条按层数从底部向上点亮
- 火/暗印记的倒计时数字直接压在各自计量条中间显示
- 右侧显示两行推荐轨道，格式为图标 + 技能名称
- 上方轨道在玩家读条/引导时变成当前施法进度条，类似卡拉 OK 时间线
- 如果当前施法匹配插件开始施法时的主推荐，进度条显示绿色；如果不匹配，显示红色，方便判断推荐逻辑和实战手感是否错位
- 下方轨道显示下一步预告；读条时会跳过当前正在施放的技能，尽量显示真正下一步
- 第一版推荐逻辑：
  - 优先级：暗影灼烧 + 混沌流星最高；如果暗影灼烧即将可用且长读条会错过窗口，推荐分流等待
  - 第二优先级是燃烧和混乱箭，尽量卡 CD 使用；如果燃烧即将可用，且混乱箭读条会跨过燃烧窗口，就不推荐混乱箭
  - 第三优先级是暗影烈焰；暗影烈焰需要 11 码距离，如果目标不在范围内且需要补暗层，推荐暗影箭
  - 第四优先级是献祭无缝刷新：献祭剩余小于等于当前急速下的献祭施法时间时推荐补献祭
  - 如果进入暗影灼烧 + 混沌流星窗口，只有火印记剩余不足以撑完整个爆发窗口时，献祭才会插队抢救火层
  - 混沌流星不单独推荐；只有玩家刚成功施放暗影灼烧后的短窗口内，才允许把混沌流星作为下一次主推荐
  - 暗影灼烧按 `+2 暗层` 计算；火 6 暗 4 且暗影灼烧、混沌流星都可用时，先推荐暗影灼烧，暗影灼烧成功后再推荐混沌流星
  - 暗影烈焰按立即 `+2 火层 +2 暗层` 计算；如果一发暗影烈焰能推到双 6，会推荐暗影烈焰，但不会单独推荐混沌流星
  - 暗影烈焰需要 11 码距离；如果目标不在暗影烈焰范围内且需要补暗层，推荐暗影箭
  - 火 6 暗 5 时，如果暗影灼烧可用且暗影烈焰 DOT 能覆盖混沌流星当前施法时间，会预测读条期间补到 6 暗，并按暗影灼烧 -> 混沌流星的链式主推荐处理
  - 如果即将进入暗影灼烧 + 混沌流星窗口，但暗影灼烧还在 CD，且混乱箭/烧尽等常规填充读条会超过暗影灼烧剩余 CD，会推荐分流等待窗口
  - 暗不足时优先暗影烈焰，不再把暗影灼烧当普通补层技能乱放
  - 火不足时用烧尽填充
  - 燃烧只有目标身上存在献祭时才会推荐
  - 其余情况用混乱箭、烧尽填充
- 没有目标时保持空条，不额外提示
- 读取层数时同时兼容 `C_UnitAuras` 和 `UnitDebuff` 两套路径，优先为木桩目标保证能读到 debuff 层数
- 只统计玩家自己施加的火/暗印记、献祭、暗影烈焰；团队里其他术士叠的同名 debuff 不参与推荐计算
- 对于“debuff 已存在但层数字段返回 0/nil”的情况，按至少 1 层处理，并在整列 debuff 中取最大层数
- 临时在插件下方打印目标前 12 个 debuff 的 `spellID:名字 x层数`，用于锁定暗印记真实数据
- 插件启动时强制使用代码内置的火/暗印记 ID，避免旧 SavedVariables 污染当前读取
- 支持拖动位置
- 支持按角色保存配置

## 需要进游戏校准的内容

当前只依赖两条默认 aura ID：

```text
余烬印记（火）: 1295144
暗影印记: 1295140
```

当前版本不提供斜杠命令配置，按“开箱即用”方向维护。

## 施法轨道设想

右侧推荐区的长期方向不是“静态告诉玩家下一个技能”，而是做成可追踪执行的施法轨道：

- 上方轨道表示当前正在执行的技能；玩家开始读条/引导后，推荐卡片变成横向时间线，像卡拉 OK 字幕一样跟随施法进度推进
- 下方轨道表示下一步预告；当前技能正在释放时，插件仍然继续计算下一手，减少读条结束后才反应的滞后感
- 施法开始瞬间记录“当时插件推荐的主技能”，用于判断玩家是否按推荐执行，而不是事后用新的推荐结果倒推
- 如果当前施法匹配推荐，进度条用绿色；如果不匹配，用红色，方便回看是玩家临场调整，还是推荐逻辑确实不合实战
- 后续可以基于这套轨道继续做“预演队列”：例如献祭读条期间预测献祭落地、燃烧变为可用、暗影烈焰 DOT 跳暗层，从而更早给出下一手
- 这套机制的核心价值是帮助调优推荐算法：当红色经常出现，说明插件推荐和玩家判断有分歧，应优先分析当时的火/暗层数、CD、射程、献祭剩余和团队内其他术士 debuff 干扰

## 推荐验证步骤

1. 将仓库内 `BigMeteor` 同步到游戏 `AddOns/BigMeteor`。
2. `/reload`
3. 选中带有火/暗印记的目标
4. 观察：
   - 左侧红色计量条是否正确对应火印记层数
   - 右侧紫色计量条是否正确对应暗印记层数
   - 切换目标或丢失目标后层数是否清空

## 下一步待办

1. 进游戏验证右侧推荐图标是否能按 CD 和当前层数正确变化。
2. 验证右侧“图标 + 技能名”是否足够清楚。
3. 继续细化暗影烈焰后续 DOT 每跳 `+1 暗层` 的短窗口模拟，目前只按“读条期间至少补 1 层”做保守预测。
4. 按实战手感微调推荐优先级。
5. 确认推荐稳定后删除下方临时 debuff 调试列表。
